import React from 'react';

const Audio = ({ peerId, debug }) => {
  // Lógica y funciones específicas para el componente de audio

  return (
    <div>
      {/* Contenido del componente de audio */}
    </div>
  );
};

export default Audio;
