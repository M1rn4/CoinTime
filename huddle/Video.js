import React from 'react';

const Video = ({ peerId, debug }) => {
  // Código para renderizar el componente de video

  return (
    <div>
      {/* Contenido del componente de video */}
    </div>
  );
};

export default Video;
